# 用户级运行时覆盖（优先级高于模型描述符 ModelDescriptor.sources 内置源）
# 取消注释并设置以下变量以覆盖默认下载配置：

# 强制使用指定的模型下载 URL（将作为第一优先源，替代 ModelDescriptor.sources 中的所有源）
# MODEL_URL=https://your-cdn.example.com/models/model.onnx

# 强制使用指定的模型文件名（替代 ModelDescriptor.filename）
# MODEL_FILENAME=custom-model.onnx
